<% extend JbuilderSchema %>

Scaffolding::CompletelyConcrete::TangibleThing::Attributes:

  <%= jbuilder_schema "api/v1/scaffolding/completely_concrete/tangible_things/_tangible_thing",
                      format: :yaml,
                      paths: view_paths.map(&:path),
                      model: current_model,
                      title: t("#{current_model.name.underscore.pluralize}.label"), # Here we'd need a singular form
                      description: "No description", # Find where the description for what TangibleThing is
                      locals: {
                        tangible_thing: FactoryBot.build(:scaffolding_completely_concrete_tangible_thing, id: 1, created_at: Time.now, updated_at: Time.now)
                      }
   %>

Scaffolding::CompletelyConcrete::TangibleThing::Parameters:
  type: object
  properties:
    <%# 🚅 skip this section when scaffolding. %>
    <%= parameter :text_field_value %>
    <%# 🚅 stop any skipping we're doing now. %>
    <%# 🚅 super scaffolding will insert new parameter above this line. %>
